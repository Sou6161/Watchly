# Expo pins

This app targets **Expo SDK 54** (see `package.json`), chosen so it runs in the
current Expo Go client. Read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code — APIs move
between SDKs.
